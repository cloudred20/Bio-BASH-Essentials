# Bio-BASH-Essentials
Fundamental BASH shell scripts designed to perform essential, daily command-line tasks. 
Before executing any shell script for the first time, run dos2unix scriptname.sh if the script in written in Notepad ++ (Windows). This step will convert the line break format to the one readable by UNIX/Linux.
