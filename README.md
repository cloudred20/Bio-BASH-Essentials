# Bio-BASH-Essentials
Fundamental BASH shell scripts designed to perform essential, daily command-line tasks.  

If you edit or download any BASH script on a Windows machine (e.g., using Notepad++), you must run dos2unix scriptname.sh before first execution on a UNIX/Linux system. This converts Windows line endings (CRLF) to the required UNIX format (LF).
